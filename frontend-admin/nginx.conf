server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # SPA-fallback: skicka alla vägar till index.html så React Router kan ta över.
    location / {
        try_files $uri /index.html;
    }

    # Standard-cache för statiska filer kan läggas till vid behov.
}
